{
  "name": "@agidb/on-chain",
  "version": "0.1.0",
  "description": "TypeScript client for the AGIDB On-Chain HTTP API",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "README.md", "LICENSE"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["blockchain", "database", "evm", "crypto", "merkle", "web3"],
  "license": "Apache-2.0",
  "engines": {"node": ">=18"},
  "devDependencies": {"typescript": "^5.6.3"}
}
